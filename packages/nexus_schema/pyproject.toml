[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nexus_schema"
version = "0.1.0"
description = "nexus_schema package"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.0"]

[tool.setuptools]
packages = ["nexus_schema"]

[project]
name = "nexus-engine"
version = "0.1.0"
description = "Nexus Engine Monorepo Workspace"
requires-python = ">=3.12"
dependencies = ["pytest>=8.0", "filelock>=3.12"]

[tool.uv.workspace]
members = ["packages/*"]

